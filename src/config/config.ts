export default () => ({
  PORT: process.env.PORT || 4000,
  // DATABASE: process.env.URI,
  // EMAIL_PROVIDER: process.env.EMAIL_PROVIDER,
  // PASSWORD_EMAIL_PROVIDER: process.env.PASSWORD_EMAIL_PROVIDER,
});
